import React from 'react';
import UnifiedLearningPlatform from '../components/UnifiedLearningPlatform';
import '../styles/modules.css';
import '../styles/premium.css';

/**
 * Page Apprendre - Version Unifiée
 * 
 * Cette page présente un parcours d'apprentissage structuré et progressif
 * pour maîtriser le data marketing, avec un mode enseignant intégré.
 */
const Learn: React.FC = () => {
  return <UnifiedLearningPlatform />;
};

export default Learn; 